/**
 * Created by Administrator on 2018/4/20.
 */
/**
 * Created by Administrator on 2018/4/20
 *  This is a function to feedback the place where have err when compile.
 *  The name of this fuction is moonlight_function_errplace.
 *  usage:
 *  1,install this function from npm before use it.
 *      such as: npm install moonlight_funciotn_errplace
 *  2,require it in the program
 *
        var errPlace=require("moonlight_function_errplace");
        var textPlace="connectdb";
        var connectDb=function(dbUrlMongodb,dbName,callback){

            try{
                ......
                });
        }
        catch(err){
            errPlace(err,textPlace);
        }
    }
    module.exports=connectDb;
 *
 */
"use strict";
var errPlace = function(err,textPlace){
    var text="There is a err at " + textPlace + ":\n\n";
    text = text + err.message + "\n\n";
    console.log(text);
}
module.exports = errPlace;
